type InventoryItem @model @auth(rules: [{allow: public, provider: apiKey, operations: [read]},
  {allow: groups, operations: [create, read, update, delete], groups: ["Admin"]}])
{
  name: String!
  price: Float
  status: String
  image: String
  description: String
}